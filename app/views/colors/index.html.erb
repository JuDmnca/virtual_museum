<p id="notice"><%= notice %></p>

<h1>Colors</h1>

<table class="table">
  <thead>
    <tr>
      <th>Image</th>
      <th>Color</th>
      <th>History</th>
      <th>Symb</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @colors.each do |color| %>
      <tr>
        <td><%= image_tag color.image %></td>
        <td><%= color.color %></td>
        <td><%= color.history %></td>
        <td><%= color.symb %></td>
        <td><%= link_to 'Show', color %></td>
        <td><%= link_to 'Edit', edit_color_path(color) %></td>
        <td><%= link_to 'Destroy', color, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Color', new_color_path %>
